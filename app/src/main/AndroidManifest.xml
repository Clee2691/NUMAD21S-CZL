<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.numad21s_czl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_czl"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_czl_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SCZL">
        <activity android:name=".ActivityLinkCollector"></activity>
        <activity android:name=".ActivityClickLetters" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>